# Copy this to .env and fill in your keys
# Keys are stored in localStorage in the browser — this file is for reference only

# 0G Compute — primary AI (required)
# Setup: pnpm add @0glabs/0g-serving-broker -g && 0g-compute-cli ui start-web
# Then connect wallet at http://localhost:3090 to get your provider URL + API key
VITE_0G_PROVIDER_URL=https://...
VITE_0G_API_KEY=app-sk-...

# 0G Storage — decentralized search history (optional)
# Get testnet tokens free at https://faucet.0g.ai (0.1 0G/day)
VITE_0G_PRIVATE_KEY=

# Claude — optional fallback AI
VITE_CLAUDE_API_KEY=sk-ant-...
# Serper — live transfer partner search (optional)
VITE_SERPER_API_KEY=...
# Seats.aero — live flight data (optional)
VITE_SEATS_API_KEY=...
